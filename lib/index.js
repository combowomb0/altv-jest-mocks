const mockClient = require('./mocks/alt-client');
const mockServer = require('./mocks/alt-server');
const mockNatives = require('./mocks/natives');

module.exports = { mockClient, mockServer, mockNatives };
